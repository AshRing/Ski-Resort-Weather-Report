import React from 'react';

const Landing = () => (
    <div className="landing">
        <h1>Welcome!</h1>
        <h2>Please pick a resort from the list to get its current and projected weather forecast.</h2>
    </div>
);

export default Landing;